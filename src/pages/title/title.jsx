/* eslint-disable react/prop-types */
import styles from './title-style/title.module.css';

const  Title = ({pageName}) => {
    return ( <h2 className={styles._title}>{pageName}</h2>)}
    

export default Title  