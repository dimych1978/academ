import React from "react";
import ReactDOM from "react-dom/client";
import { AppRouters } from "./routes.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AppRouters />
  </React.StrictMode>
);
